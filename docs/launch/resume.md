# Resume copy

Use the version that best fits the available space. These statements describe
the documented v0.x scope and avoid unverified usage or performance claims.

## Version A — one bullet

- Built **Conduit**, an open-source Python MCP gateway that federates multiple downstream servers through deterministic routing, default-deny policy, credential isolation, and durable audit-before-dispatch controls; published it to PyPI with CI-tested documentation and release automation.

## Version B — two bullets

- Built and open-sourced **Conduit**, a local-first Python gateway that exposes multiple MCP servers through one namespaced tool catalog and exact stored routes.
- Implemented safety-critical gateway controls: centralized deny-wins/default-deny policy, per-server credential isolation, durable authorization auditing before dispatch, bounded responses, and no automatic tool-call replay; distributed the package on PyPI with automated quality checks.

## Version C — compact project entry

**Conduit** — Python, MCP, HTTP/JSON-RPC, PyYAML, pytest, GitHub Actions, MkDocs, PyPI

- Designed a local-first MCP gateway that federates multiple Streamable HTTP servers into deterministic namespaced tools while preserving exact downstream routing.
- Added fail-closed policy and audit boundaries, credential/header isolation, response limits, and compatibility coverage for standard HTTP MCP clients; packaged and documented the project for open-source distribution.
